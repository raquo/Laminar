package com.raquo.laminar.experimental.airstream.core

import org.scalatest.{FunSpec, Matchers}

class SyncDependsOnSpec extends FunSpec with Matchers {

  // @TODO hmmm we're actually testing SingleParentSyncObservable, it seems

  ignore("EventStream depends on its ancestor") { ??? }

  ignore("EventStream does not depend on unrelated Observable") { ??? }

  ignore("EventStream does not depend on its descendant") { ??? }

  ignore("EventStream does not depend on itself") { ??? }

}
